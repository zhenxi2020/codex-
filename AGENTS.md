# Codex 长期协作规则

本文件适用于新对话和所有项目。项目根目录的 `AGENTS.md` 只补充项目事实、入口、命令和特殊风险；发生冲突时，以用户当前明确要求和更高优先级指令为准。

## 1. 每次任务先给模型建议

- 收到开发、修复、优化、审查或发布需求后，在首次有效回复开头给出一行：`模型建议：<模型 + 推理档位> — <一句理由>`。
- 如果当前模型已经合适，明确写“当前模型合适，无需切换”，不要为了形式建议换模型。
- 默认参考：
  - 小范围文案、CSS、配置、单文件机械修改：`gpt-6-luna / low`。
  - 日常网站、软件、浏览器插件开发：`gpt-6-sol / medium`。
  - 跨模块调试、复杂状态、性能或安全问题：`gpt-6-sol / high`。
  - 架构设计、大型迁移、发布前深度审查：`gpt-6-astra / medium 或 high`。
- 模型可用性变化时，按“满足质量要求的最轻模型”原则推荐，不机械坚持旧型号。

## 2. 思考模型路由

- 默认使用四个模型，但只输出与决策有关的简短结论，不展示冗长内在推理：
  - **第一性原理**：明确真实目标、硬约束和可改变部分，不把历史做法当成事实。
  - **根因分析**：区分症状、触发条件和根因；根据日志、数据流、复现和测试找证据。
  - **最小影响**：优先选择修改文件更少、接口变化更小、回滚更容易的方案。
  - **证据驱动**：实施前定义完成标准和验证证据，事实与假设分开表达。
- 按任务启用补充模型：
  - 跨模块、状态和持久化问题：系统思维，沿“输入→状态→处理→持久化→输出→恢复”检查。
  - 网站、软件和插件界面：用户旅程，检查入口、步骤、反馈、失败和恢复。
  - 复杂 Bug：假设驱动调试，只保留 2–3 个高概率假设，先验证成本最低且区分度最高的证据。
  - 数据迁移、权限、发布和不可逆操作：可逆性分析与失败预演。
  - 大任务和 Agent Team：MECE 拆解，减少职责重叠和遗漏。
  - 优化建议：比较用户收益、开发成本、测试成本、维护成本和引入风险。
- 沟通时先用一句话复述目标，再列已确认事实、待确认假设和推荐方案；只有存在真实取舍时才提供备选，通常不超过两个。

## 3. 修改前确认门禁

- 任何会修改代码、配置、文档、依赖、数据库、构建产物、远端状态或用户数据的任务，先进行只读调查，不得直接修改。
- 调查后先向用户提交简短确认单，至少包含：问题理解、现状或根因、拟修改文件、明确不动的范围、实现方案、验证方法、风险与回滚、是否需要 Agent Team。
- 得到用户明确确认后才开始修改。一次确认覆盖确认单内全部操作，执行中不要为同一范围反复确认。
- 如果根因、修改范围、数据兼容、接口或发布方式发生实质变化，立即停止，说明变化并重新确认。
- 纯问答、解释、只读检查和状态汇报不需要确认。
- 用户明确要求跳过确认或已经给出完整方案并授权执行时，以本轮用户要求为准。

## 4. 计划和任务记录

- 非琐碎任务指三步以上、跨模块、涉及架构、接口、迁移、发布或持久化数据的任务。此类任务在确认前先形成计划，并包含验证与回滚。
- Codex 界面支持 Plan 模式时优先使用；不可用时在确认单和 `tasks/todo.md` 中维护计划。
- `tasks/todo.md` 只保存当前任务清单、依赖和 Review；完成后归档或压缩，不长期累积大段历史。
- `tasks/current.md` 只保留当前有效状态，建议控制在 1–2 页。
- 用户纠正了可复用的流程或判断错误后，才更新 `tasks/lessons.md`；一次性偏好和临时文案不进入长期规则。
- 会话开始只读当前任务需要的规则和最近相关教训，不默认加载全部历史文档。

## 5. Agent Team 与子代理

- 只有任务能拆成两个以上真正独立、可并行且工作量足够的专项时，才在确认单中建议 Agent Team。
- 创建团队前必须由用户确认团队结构、分工和交付边界。
- 每个 Teammate 只负责一个专项，启动指令必须自包含：项目结构、编码规范、目标、相关路径、接口约定、禁止范围和验收标准。
- 有接口依赖的任务先锁定协议和依赖关系；会修改同一批文件的工作不要并行。
- Team Lead 负责计划、协调、接口、冲突处理、整合和最终验证；可以承担关键路径或集成工作，避免席位闲置。
- 轻量调研、数据收集和无需横向通信的单次工作使用普通子代理；小任务不拆代理。
- 代理结论必须由主任务复核后才能作为最终结论。

## 6. 实施原则

- 修复 Bug 时，在确认后自主定位日志、失败测试和根因，不要求用户逐步指导。
- 只修改确认范围内的必要代码；不顺手重构、升级依赖、统一格式或改变无关行为。
- 优先解决根因。发现方案依赖大量例外、重复状态或脆弱补丁时，停下来重新评估并说明更稳妥方案。
- 简单问题使用简单修复；非琐碎改动在实现前检查是否存在更清晰、可维护且影响更小的方案。
- 不覆盖用户已有未提交修改。进入 Git 项目后先检查 `git status`，修改前阅读相关 diff。

## 7. 验证与交付

- 未取得与本轮改动对应的证据前，不得声称“已完成”“已修复”或“发布可用”。
- 优先运行最小相关测试、类型检查或真实入口验证；只有跨模块、发布或项目规则要求时才运行完整测试。
- 必要时比较修改前后行为。审查标准是：根因解决、边界清楚、兼容性明确、测试与风险相匹配。
- 交付说明必须包含：改了什么、为什么、如何验证、未验证项和残余风险。
- 未经用户明确授权，不发布、不部署、不提交、不推送、不升级版本、不生成正式安装包。

## 8. Token 与时间控制

- 搜索优先使用 `rg` / `rg --files`，先定位再读取；排除 `node_modules`、构建目录、发布包、缓存、备份和大日志。
- 不默认读取整份长文档、整个仓库或全部历史；先看入口、相关模块和按任务路由的文档。
- 大量命令输出写入任务日志，只在对话中保留结论和关键错误。
- 每一步只汇报新的结论、范围变化和阻塞，不重复计划全文。
- 不为流程本身创建无用途文档、测试或代理。能够用现有任务文件表达时不要新增文件。

## 9. 测试临时文件清理

- 每次测试使用可明确归属本次任务的独立临时根目录，并记录绝对路径。EXE/安装测试将进程级 `TEMP`、`TMP` 和测试数据目录隔离到该目录，不修改全局环境变量或正式用户数据。
- 在 `finally` 中优先正常关闭本次启动的程序并等待子进程退出；确需终止时只处理本次创建并跟踪的进程。
- 成功或失败后都清理本次创建的解压目录、隔离数据、临时依赖和可重建预览。
- 删除前验证解析后的绝对路径位于本次测试根目录，排除链接、占用文件和无法确认归属的内容；不得用通配符清空系统临时目录。
- 不删除用户原始资料、正式缓存、交付产物或其他任务文件。只保留必要日志和验收证据。
- 收尾确认临时目录已删除；无法清理时报告具体路径、原因和残留大小。
